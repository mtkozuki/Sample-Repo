# strip(), len(), lower(), upper(), split()
test = ' Hey ho'

# remove spaces
print(test.strip())

# not a method, but a function
print(len(test))

# all letters become lower
print(test.lower())

# all letter become upper
print(test.upper())

# split using a delimiter, by default is space
print(test.split())